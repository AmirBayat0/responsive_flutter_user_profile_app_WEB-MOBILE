import 'package:flutter/material.dart';

class LightColors {
  static const bgColor = Color.fromARGB(255, 245, 245, 245);
  static const titleColor = Colors.black;
  static const subTitleColor = Colors.grey;
}

class DarkColors {
  static const bgColor = Color.fromARGB(255, 18, 18, 18);
  static const titleColor = Colors.white;
  static const subTitleColor = Colors.white60;
}
